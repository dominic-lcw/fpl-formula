name: Deploy FPL Formula

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: fpl-formula-production
  cancel-in-progress: true

jobs:
  deploy:
    environment: production
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.3

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests and lint checks
        run: |
          pnpm test
          pnpm lint

      - name: Hydrate FPL data
        run: pnpm hydrate

      - name: Build standalone server
        run: |
          pnpm build
          rm -rf deployment
          mkdir deployment
          cp -RL .next/standalone/. deployment/
          helpers_package="$(node -e "const path = require('node:path'); const nextPackage = require.resolve('next/package.json'); console.log(require.resolve('@swc/helpers/package.json', { paths: [path.dirname(nextPackage)] }))")"
          mkdir -p deployment/node_modules/@swc
          cp -RL "$(dirname "$helpers_package")" deployment/node_modules/@swc/helpers
          mkdir -p deployment/.next
          cp -R .next/static deployment/.next/static
          if [ -d public ]; then cp -R public deployment/public; fi
          cp -R data deployment/data
          cd deployment
          zip -qr ../fpl-formula.zip .

      - name: Sign in to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Configure standalone startup command
        run: >
          az webapp config set
          --resource-group "${{ vars.AZURE_RESOURCE_GROUP }}"
          --name "${{ vars.AZURE_WEBAPP_NAME }}"
          --startup-file "node server.js"

      - name: Deploy package to App Service
        run: >
          az webapp deploy
          --resource-group "${{ vars.AZURE_RESOURCE_GROUP }}"
          --name "${{ vars.AZURE_WEBAPP_NAME }}"
          --src-path fpl-formula.zip
          --type zip
          --async false
